#!/bin/sh

cat <<EOF
WARNING: the init script /etc/init.d/openssh doesn't exist anymore !
under Solaris < 10:
	it has been renamed /etc/init.d/cswopenssh
under Solaris >= 10:
	it has been replaced by the SMF system,
	use svcadm enable|disable svc:/network/cswopenssh:default
EOF
