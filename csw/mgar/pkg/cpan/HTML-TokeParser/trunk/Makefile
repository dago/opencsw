GARNAME = HTML-TokeParser-Simple
GARVERSION = 3.15
CATEGORIES = cpan
AUTHOR = OVID

DESCRIPTION = HTML::TokeParser - Alternative HTML::Parser interface
define BLURB
The HTML::TokeParser is an alternative interface to the HTML::Parser
class. It is an HTML::PullParser subclass with a predeclared set of
token types. If you wish the tokens to be reported differently you
probably want to use the HTML::PullParser directly.
endef

DISTFILES += CSWpmhtmltokeparser.gspec CSWpmhtmltokeparser.depend

DEPENDS += cpan/HTML-Parser

include ../category.mk
