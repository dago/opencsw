NAME = Passwd-Unix
VERSION = 0.52
CATEGORIES = cpan
GARTYPE = v2
AUTHOR = STRZELEC

DESCRIPTION = Abstract interface to /etc/passwd, /etc/shadow and /etc/group format files
define BLURB
  Abstract interface to /etc/passwd, /etc/shadow and /etc/group format files
endef

DISTFILES += COPYING

PACKAGES = CSWpmpasswdunix
CATALOGNAME = pm_passwdunix
ARCHALL = 1

RUNTIME_DEP_PKGS += CSWpmcryptpasswdmd5 CSWpmstructcompare

include gar/category.mk
