GARNAME = Passwd-Unix
GARVERSION = 0.52
CATEGORIES = cpan
AUTHOR = STRZELEC

DESCRIPTION = Abstract interface to /etc/passwd, /etc/shadow and /etc/group format files
define BLURB
  Abstract interface to /etc/passwd, /etc/shadow and /etc/group format files
endef

DISTFILES += COPYING

PACKAGES = CSWpmpdfreuse
CATALOGNAME = pm_pdfreuse
ARCHALL = 1

include gar/category.mk
