NAME = Sys-RunAlone-User
VERSION = 0.01
CATEGORIES = cpan
GARTYPE = v2
AUTHOR = PEDLAR

DESCRIPTION = Make sure only one invocation of a script is active at a time per user
define BLURB
endef

CATALOG_RELEASE = unstable

LICENSE = LICENSE

PACKAGES += CSWpm-sys-runalone-user
CATALOGNAME_CSWpm-sys-runalone-user = pm_sys_runalone_user
SPKG_DESC_CSWpm-sys-runalone-user = Make sure only one invocation of a script is active at a time per user
# There was no information if this is pure Perl or not. Please remove if necessary.
ARCHALL_CSWpm-sys-runalone-user = 1

include gar/category.mk
