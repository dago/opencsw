NAME = XML-Filter-BufferText
VERSION = 1.01
GARTYPE = v2
CATEGORIES = cpan
AUTHOR = RBERJON

DESCRIPTION = Filter to put all characters() in one event
define BLURB
  Filter to put all characters() in one event
endef

DISTFILES += COPYING

PACKAGES = CSWpm-xml-filter-buffertext
CATALOGNAME = pm_xml_filter_buffertext
ARCHALL = 1

RUNTIME_DEP_PKGS += CSWpm-xml-sax
RUNTIME_DEP_PKGS += CSWpm-xml-sax-base

CONFIGURE_ARGS = 

include gar/category.mk
