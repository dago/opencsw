NAME = Email-Send
VERSION = 2.199
CATEGORIES = cpan
GARTYPE = v2
AUTHOR = RJBS

DESCRIPTION = Simply Sending Email
define BLURB
endef

LICENSE = LICENSE

PACKAGES = CSWpm-email-send
CATALOGNAME = pm_email_send

RUNTIME_DEP_PKGS += CSWpm-email-abstract
RUNTIME_DEP_PKGS += CSWpm-email-address
RUNTIME_DEP_PKGS += CSWpm-email-simple
RUNTIME_DEP_PKGS += CSWpm-return-value

ARCHALL = 1

include gar/category.mk
