NAME = Hash-Merge-Simple
VERSION = 0.051
CATEGORIES = cpan
GARTYPE = v2
AUTHOR = ROKR

DESCRIPTION = Recursively merge two or more hashes, simply
define BLURB
endef


PACKAGES += CSWpm-hash-merge-simple
CATALOGNAME_CSWpm-hash-merge-simple = pm_hash_merge_simple

ARCHALL = 1

BUILD_DEP_PKGS += CSWpm-test-most

RUNTIME_DEP_PKGS += CSWpm-clone

include gar/category.mk
