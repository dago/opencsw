# $Id$
# TODO (release-critical prefixed with !, non release-critical with *)
#
NAME = WSGIProxy2
VERSION = 0.1
CATEGORIES = python
GARTYPE = v2

DESCRIPTION = WSGI Proxy with various http client backends

DISTFILES = $(DISTNAME).tar.gz
VENDOR_URL = https://pypi.python.org/pypi/WSGIProxy2
PACKAGES = CSWpy-wsgiproxy2
ARCHALL_CSWpy-wsgiproxy2 = 1

# There is no testsuite
TEST_SCRIPTS =

include gar/category.mk
