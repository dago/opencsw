#!/bin/sh

BINDIR=$BASEDIR/opt/csw/bin

cd $BINDIR
rm -f sudo sudoedit

OTHERSUDO=`ls -1 sudo.* 2>/dev/null | head -1`
if [ -n "$OTHERSUDO" ]; then
    rm -f sudo
    ln -f $OTHERSUDO sudo
    ln -f $OTHERSUDO sudoedit
fi

exit 0

