ISA=all for phases fetch - checksum and package

# ISA_RELOCATE_FILES wird noch nicht benutzt, nur ISA_RELOCATE_DIRS

- documentation
- Using Multi ISA to build iozone
  - modulate over
        @echo "        ->   Solaris              (32bit)   <-"
        @echo "        ->   Solaris-2.6          (32bit)   <-"
        @echo "        ->   Solaris7gcc          (32bit)   <-"
        @echo "        ->   Solaris8-64          (64bit)   <-"
        @echo "        ->   Solaris8-64-VXFS     (64bit)   <-"
        @echo "        ->   Solaris10            (32bit)   <-"
        @echo "        ->   Solaris10cc          (64bit)   <-"
        @echo "        ->   Solaris10gcc         (32bit)   <-"
        @echo "        ->   Solaris10gcc-64      (64bit)   <-"
- libncurses amd64 fuer sysstat
- TCL amd64 fuer Kerberos
- Alex Moore wegen:
  - SOLARISROOTDSE: Problem bei Solaris 8
  - 64 Bit OpenLDAP
- Maintainer HylaFAX, neue Version?
- iometer support fuer amd64

- OpenSOLARIS SMF Book runterladen
  - Doku von Christian?
