GARNAME = Class-ReturnValue
GARVERSION = 0.53
CATEGORIES = cpan
AUTHOR = JESSE

DESCRIPTION = a return-value object.
define BLURB
  Class::ReturnValue is a return value object that can allow code calling your
  routine to expect: a boolean value (did it fail) or a list (what are the
  return values)
endef

DISTFILES += CSWpmclassretval.gspec CSWpmclassretval.depend

DEPENDS   += cpan/Devel-StackTrace
BUILDDEPS += cpan/Test-Inline

include ../category.mk
