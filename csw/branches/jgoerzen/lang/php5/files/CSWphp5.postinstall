#!/bin/sh

PKG_INSTALL_ROOT=${PKG_INSTALL_ROOT:-'/'}
PHP_INI=$PKG_INSTALL_ROOT/opt/csw/php5/lib/php.ini

if [ ! -f $PHP_INI ]; then
    cp -p $PHP_INI.CSW $PHP_INI
else
    echo "php.ini already exists.  Not copying php.ini.CSW"
fi

exit 0
