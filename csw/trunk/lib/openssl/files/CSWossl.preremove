#!/bin/sh

DATE=`/bin/date '+%Y%m%d%H%M%S'`

/bin/find ${BASEDIR}/opt/csw/lib -type f -name libcrypto.so.?.?.? -o -name libssl.so.?.?.? | while read FILE;
	do /bin/mv "$FILE" "${FILE}.${DATE}.$$"
done
