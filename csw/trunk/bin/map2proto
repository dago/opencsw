#!/opt/csw/bin/perl -lw
#
# $Id$
# $URL$
#
# map2proto - turn a pkgmap into a prototype
#

use strict;

while (<>) {
    chomp ; my @F = split;
    next unless @F > 3;
    print join(" ", ($F[1] =~ /f|d|i/) ? @F[1..$#F-3] : @F[1..$#F]);
}
