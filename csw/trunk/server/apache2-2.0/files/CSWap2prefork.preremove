#!/bin/sh

CSW_PREFIX=${PKG_INSTALL_ROOT}/opt/csw
AP2_PREFIX=$CSW_PREFIX/apache2
AP2_BINDIR=$AP2_PREFIX/sbin

# Stop apache
if [ -z "$PKG_INSTALL_ROOT" ]; then
    echo Stopping apache...
    $AP2_BINDIR/apachectl stop
    sleep 2
fi

exit 0
