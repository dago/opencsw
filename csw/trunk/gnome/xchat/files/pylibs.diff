diff --speed-large-files --minimal -Nru xchat-2.8.0.orig/configure xchat-2.8.0/configure
--- xchat-2.8.0.orig/configure	2007-01-02 22:35:40.000000000 -0500
+++ xchat-2.8.0/configure	2007-02-20 23:27:09.630708000 -0500
@@ -26970,7 +26970,7 @@
                         PY_PREFIX=`$pythonpath -c 'import sys; print sys.prefix'`
                         PY_EXEC_PREFIX=`$pythonpath -c 'import sys; print sys.exec_prefix'`
                         if test -f $PY_INC/Python.h; then
-                                PY_LIBS="-L$PY_LIB/config -lpython$PY_VER -lpthread -lutil"
+                                PY_LIBS="-L$PY_LIB/config -lpython$PY_VER -lpthread -lrt -lm"
                                 PY_CFLAGS="-I$PY_INC"
                                 echo "$as_me:$LINENO: result: ok" >&5
 echo "${ECHO_T}ok" >&6
